# Quiz Backend (.env.example)
# Copy this file to .env and adjust values as needed.

# MySQL connection settings (required)
MYSQL_HOST=localhost
MYSQL_PORT=5000
MYSQL_DB=myapp
MYSQL_USER=appuser
MYSQL_PASSWORD=dbuser123

# Optional: FastAPI/uvicorn server settings (can also be passed via CLI)
# UVICORN_HOST=0.0.0.0
# UVICORN_PORT=8000
# UVICORN_RELOAD=true
